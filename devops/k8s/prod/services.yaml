apiVersion: v1
kind: Service
metadata:
  name: gerenciador-oficina-notificacao-service
spec:
  type: LoadBalancer
  selector:
    app: gerenciador-oficina-notificacao-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
